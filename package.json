{
  "name": "pullrequests",
  "version": "1.0.0",
  "description": "macOS menu bar app for monitoring GitHub pull requests",
  "scripts": {
    "dev": "swift build && swift run",
    "build": "swift build",
    "build:release": "bash scripts/build-app.sh",
    "install": "bash scripts/install.sh",
    "clean": "rm -rf .build/"
  }
}
